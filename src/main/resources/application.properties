spring.config.import=optional:classpath:env.properties
# Default config for running locally without Docker
# When running inside Docker, SPRING_MONGODB_URI env variable overrides this
spring.mongodb.uri=${MONGO_URI}
spring.application.name=election-app